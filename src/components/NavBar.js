import React from "react";

function NavBar() {
  // update the JSX being returned!
  return <nav>
    <nav>
      <a href="#home">Home</a>
      <a href="#about">About</a>
    </nav>
  </nav>;
}

export default NavBar;
